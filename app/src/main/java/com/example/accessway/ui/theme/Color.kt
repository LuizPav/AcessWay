package com.example.accessway.ui.theme

import androidx.compose.ui.graphics.Color

val LogoBlue = Color(0xFF0F4392)       // O azul escuro marcante do pin da logo
val LogoGreen = Color(0xFF62B034)      // O verde claro/médio da base do pin da logo

// 🎨 Cores de Ação e UI (Vindas do seu código)
val PrimaryButtonBlue = Color(0xFF1976D2) // O azul do botão de login
val LinkBlue = Color(0xFF1A73E8)          // O azul do texto "registre-se"
val CustomGreen = Color(0xFF43A047)       // O verde (0xFF43A047) enviado no prompt

// 🎨 Tons Neutros (Para fundos, superfícies e tipografia)
val BackgroundWhite = Color(0xFFFFFFFF)   // Fundo branco limpo que você usa nas telas
val TextDarkGray = Color(0xFF444444)      // Cinza escuro para títulos ("Bem-vindo")
val TextMediumGray = Color(0xFF555555)    // Cinza intermediário ("Ainda não possui conta?")
val TextLightGray = Color(0xFF888888)     // Cinza claro para placeholders e bordas secundárias
val ErrorRedViva = Color(0xFFE53935)